services:
  order-system-backend:
    container_name: order-system-backend
    image: ghcr.io/zl-order-system/backend:latest
    networks:
      - nginx_proxied

networks:
  nginx_proxied:
    external: true
